<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.PracticeApplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeApplication"
        android:usesCleartextTraffic="true">
        <activity
            android:name="com.example.PracticeApplication.AuthenticationActivity"
            android:label="@string/title_activity_authentication"
            android:theme="@style/Theme.PracticeApplication.NoActionBar"></activity>
        <activity android:name="com.example.PracticeApplication.ThirdActivity" />
        <activity android:name="com.example.PracticeApplication.SecondActivity" /> <!-- first activity==MainActivity -->
        <activity
            android:name="com.example.PracticeApplication.MainActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.PracticeApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>